# Styles

This directory contains the global styling architecture for the frontend.

## Responsibilities

- Global styles
- Theme definitions
- Design tokens
- CSS custom properties
- Future utility styles

The design system follows a CSS-first approach using CSS custom properties so that Tailwind CSS and Shadcn UI share the same source of truth.
